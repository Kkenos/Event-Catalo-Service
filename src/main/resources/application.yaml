spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/event_catalog
    username: postgres
    password: G7h&3jK9#lM2$nP
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect